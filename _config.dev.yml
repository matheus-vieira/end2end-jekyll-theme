host: 0.0.0.0
livereload: true
# force_polling increases CPU usage and can make laptops warm.
# Enable only when the filesystem doesn't emit change events (WSL2, Docker
# mounts, NFS/SMB, or some remote/editor setups). To enable temporarily,
# run: `jekyll serve --force_polling`
# force_polling: true
incremental: true
url: "http://localhost:4000"
baseurl: ""

# Google Analytics configuration (optional)
ga_measurement_id: ""
# Example GA options:
ga_options: "" # {"anonymize_ip": true, "cookie_expires": 0}
