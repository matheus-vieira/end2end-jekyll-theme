# `rake serve`

Installs dependencies, generates required assets, and starts a local Jekyll
server. Preferred for local development because it ensures generated Sass
partials exist before Jekyll compiles.

Usage:

```bash
bundle exec rake serve
```
